server:
  port: 8088
spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/minio_db
    username: user
    password: password
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
minio:
  login: minioadmin
  password: minioadmin
  url: http://localhost:9000
  bucket: first-bucket

